import styled from 'styled-components';

export const Button = styled.button`
  font-weight: bold;
  color: var(--secondary-color);
  background-color: var(--brown-label-color);
  border-radius: 20rem;
  padding: 0.5rem 1.25rem;
  border: none;
  outline: none;
  cursor: pointer;
`;
