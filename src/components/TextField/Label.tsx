import styled from "styled-components";

const Label = styled.label`
	position: absolute;
	top: -1.25rem;
	font-weight: 700;
	font-size: 1rem;
	line-height: 1.2;
	color: #342f29;
`;

export default Label;
